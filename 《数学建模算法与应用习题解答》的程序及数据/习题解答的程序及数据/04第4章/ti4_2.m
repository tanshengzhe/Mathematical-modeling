clc, clear
a=zeros(5); %邻接矩阵初始化
a(1,[2:5])=[0.8 2 3.8 6]; %输入邻接矩阵，注意这里实际上为有向图
a(2,[3:5])=[0.9 2.1 3.9];
a(3,[4,5])=[1.1 2.3]; a(4,5)=1.4;
b=sparse(a); %有向图直接变成稀疏矩阵就可以了
[dist,path]=graphshortestpath(b,1,5,'Directed',1) %调用工具箱求最短路
